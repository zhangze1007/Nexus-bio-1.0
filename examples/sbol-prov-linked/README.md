# SBOL And PROV Linked Example

This directory contains small illustrative JSON examples that link an SBOL-aligned artifact to a Nexus trust-runtime provenance bundle.

These examples are not full SBOL or PROV-DM validator fixtures. They are sidecar examples for the protocol foundation in:
- `spec/sbol-3-mapping.md`
- `spec/prov-dm-mapping.md`

Use them as examples of alignment vocabulary only.
