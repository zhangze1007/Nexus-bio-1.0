fastapi==0.115.0
uvicorn[standard]==0.30.0
python-multipart==0.0.9
anndata==0.10.9
scanpy==1.10.3
squidpy==1.6.1

# Leiden clustering — GPL-3.0 (strong copyleft)
# For SaaS deployment: GPL does NOT trigger (no distribution occurs)
# For distribution: replace with python-igraph + louvain (MIT) or graspologic (BSD-3)
# See THIRD_PARTY_LICENSES.md for details
leidenalg==0.10.2

numpy>=1.24.0
scipy>=1.11.0
pandas>=2.0.0
scikit-learn>=1.3.0
umap-learn>=0.5.4
Pillow>=10.0.0

# MOFA+ multi-omics — LGPL-3.0 (weak copyleft)
# SaaS-safe; for distribution ensure dynamic linking
mofapy2>=0.7.0
mofax>=0.3.0

h5py>=3.0.0
matplotlib>=3.5.0
seaborn>=0.12.0
biopython>=1.80

# ViennaRNA — MFE secondary structure prediction (RNAfold)
# Custom academic license: free for academic use; commercial requires license from University of Vienna
ViennaRNA>=2.6.0

# ESM-2 protein language model (CPU-only PyTorch)
# Install separately: pip install torch --extra-index-url https://download.pytorch.org/whl/cpu
# fair-esm>=2.0.0
